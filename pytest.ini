[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    smoke: critical API checks
    positive: successful API behavior
    negative: error handling and validation
    performance: response-time service-level checks
log_cli = true
log_cli_level = INFO
